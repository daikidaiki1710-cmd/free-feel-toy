# Resend API key used by src/app/api/contact/route.ts to send the CONTACT
# form's emails. Get one at https://resend.com/api-keys — set the real
# value in Vercel's project environment variables, never commit it here.
RESEND_API_KEY=
